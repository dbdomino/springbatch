#spring.application.name=springbatch
spring:
  batch:
    job:
      names: ${job.name:NONE}
  jdbc:
    initialize-schema: ALWAYS
  datasource:
    url: jdbc:mysql://localhost:3306/springbatch?useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: minod
  jpa:
    show-sql: true


